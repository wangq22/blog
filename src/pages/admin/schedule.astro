---
import AdminLayout from '../../layouts/AdminLayout.astro';

const apiBase = import.meta.env.PUBLIC_API_BASE || 'https://blog-api.charlie-cloud.me/api';

export const prerender = true;
---

<AdminLayout
  pageTitle="Schedule Owner Controls"
  description="Private AI-assisted schedule controls."
  canonicalPath="/admin/schedule/"
  currentLabel="Schedule"
  scheduleApiBase={apiBase}
/>
